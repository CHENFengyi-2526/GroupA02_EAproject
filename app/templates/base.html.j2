<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ASP.NET Core - Free. Cross-platform. Open-source{% endblock %}</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            padding-top: 70px;
            background-color: #f8f9fa;
        }
        .navbar-dark.bg-dark {
            background-color: #1e1e1e !important;
        }
        .navbar-brand {
            font-weight: 700;
            font-size: 1.6rem;
        }
        .navbar-brand i {
            color: #5c2d91;
        }
        .btn-purple {
            background-color: #5c2d91;
            border-color: #5c2d91;
            color: white;
        }
        .btn-purple:hover {
            background-color: #4a2373;
            border-color: #4a2373;
            color: white;
        }
        .btn-outline-purple {
            border-color: #5c2d91;
            color: #5c2d91;
        }
        .btn-outline-purple:hover {
            background-color: #5c2d91;
            color: white;
        }
        .hero {
            background: linear-gradient(135deg, #5c2d91 0%, #7b4cb6 100%);
            color: white;
            padding: 5rem 0;
            margin-bottom: 3rem;
            border-radius: 0 0 2rem 2rem;
        }
        .hero h1 {
            font-weight: 700;
            font-size: 3.5rem;
        }
        .card {
            border: none;
            border-radius: 1rem;
            transition: transform 0.2s;
        }
        .card:hover {
            transform: translateY(-5px);
        }
        .card i {
            font-size: 2.5rem;
            color: #5c2d91;
        }
        footer {
            background-color: #1e1e1e;
            color: #ccc;
            padding: 3rem 0;
            margin-top: 4rem;
        }
        footer a {
            color: #aaa;
            text-decoration: none;
        }
        footer a:hover {
            color: white;
        }
        .bg-purple {
            background-color: #5c2d91;
        }
        .text-purple {
            color: #5c2d91;
        }
        .hover-shadow {
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .hover-shadow:hover {
            transform: translateY(-4px);
            box-shadow: 0 1rem 3rem rgba(0,0,0,.15) !important;
        }
        .tutorial-content h2 {
            font-size: 1.8rem;
            margin-top: 2rem;
            margin-bottom: 1rem;
        }
        .tutorial-content pre {
            background: #1e1e1e;
            color: #d4d4d4;
            padding: 1rem;
            border-radius: 0.5rem;
            overflow-x: auto;
        }


        body.dark-mode {
            background-color: #1a1a1a;
            color: #e0e0e0;
        }
        body.dark-mode .navbar-dark.bg-dark {
            background-color: #0d0d0d !important;
        }
        body.dark-mode .card {
            background-color: #2d2d2d;
            border-color: #3d3d3d;
        }
        body.dark-mode .card-header.bg-white {
            background-color: #2d2d2d !important;
        }
        body.dark-mode .list-group-item {
            background-color: #2d2d2d;
            color: #e0e0e0;
            border-color: #3d3d3d;
        }
        body.dark-mode .bg-light {
            background-color: #2d2d2d !important;
        }
        body.dark-mode .text-muted {
            color: #aaa !important;
        }
        body.dark-mode .alert-info {
            background-color: #1a3a4a;
            color: #e0e0e0;
            border-color: #2a5a6a;
        }
        body.dark-mode footer {
            background-color: #0d0d0d;
            color: #aaa;
        }
        body.dark-mode .btn-outline-purple {
            border-color: #9b6bcb;
            color: #9b6bcb;
        }
        body.dark-mode .btn-outline-purple:hover {
            background-color: #9b6bcb;
            color: #1a1a1a;
        }


    </style>
    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('main.index') }}">
                <i class="fab fa-microsoft"></i> ASP.NET Core
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('tutorials.list') }}">Tutorials</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('resources.list') }}">Resources</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('community.index') }}">Community</a></li>
                    {% if current_user.is_authenticated and current_user.is_admin() %}
                    <li class="nav-item"><a class="nav-link" href="{{ url_for('admin.dashboard') }}">Admin</a></li>
                    {% endif %}
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="themeToggle">
                            <i class="fas fa-moon"></i>
                        </a>
                    </li>
                    {% if current_user.is_authenticated %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                
                                <i class="fas fa-user-circle"></i> {{ current_user.username }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ url_for('main.user_profile', username=current_user.username) }}">Profile</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ url_for('auth.logout') }}">Logout</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.list_categories') }}">Manage Categories</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.list_tags') }}">Manage Tags</a></li>
                                <li><a class="dropdown-item" href="{{ url_for('tutorials.create') }}">New Tutorial</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item"><a class="nav-link" href="{{ url_for('auth.login') }}">Login</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ url_for('auth.register') }}">Register</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <div class="container mt-3">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
    </div>

    <!-- Main Content -->
    <main>
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5>ASP.NET Core</h5>
                    <p class="text-muted">Free. Cross-platform. Open-source.<br>Build web apps and services with .NET and C#.</p>
                </div>
                <div class="col-md-2 mb-3">
                    <h6>Learn</h6>
                    <ul class="list-unstyled">
                        <li><a href="{{ url_for('tutorials.list') }}">Tutorials</a></li>
                        <li><a href="#">Documentation</a></li>
                        <li><a href="#">Samples</a></li>
                    </ul>
                </div>
                <div class="col-md-2 mb-3">
                    <h6>Community</h6>
                    <ul class="list-unstyled">
                        <li><a href="{{ url_for('community.index') }}">Forum</a></li>
                        <li><a href="#">GitHub</a></li>
                        <li><a href="#">Stack Overflow</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h6>Follow Us</h6>
                    <a href="#" class="text-muted me-3"><i class="fab fa-twitter fa-lg"></i></a>
                    <a href="#" class="text-muted me-3"><i class="fab fa-github fa-lg"></i></a>
                    <a href="#" class="text-muted"><i class="fab fa-linkedin fa-lg"></i></a>
                </div>
            </div>
            <hr>
            <div class="text-center text-muted small">
                &copy; 2026 ASP.NET Core Community | Rebuilt with Flask
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js">

    function setTheme(theme) {
        if (theme === 'dark') {
            document.body.classList.add('dark-mode');
            document.getElementById('themeToggle').innerHTML = '<i class="fas fa-sun"></i>';
        } else {
            document.body.classList.remove('dark-mode');
            document.getElementById('themeToggle').innerHTML = '<i class="fas fa-moon"></i>';
        }

        document.cookie = "theme=" + theme + "; path=/; max-age=" + (365 * 24 * 60 * 60);
    }


    function getCookie(name) {
        let value = "; " + document.cookie;
        let parts = value.split("; " + name + "=");
        if (parts.length === 2) return parts.pop().split(";").shift();
    }


    document.addEventListener('DOMContentLoaded', function() {
        let savedTheme = getCookie('theme');
        if (savedTheme === 'dark') {
            setTheme('dark');
        } else {
            setTheme('light');
        }
    });


    document.getElementById('themeToggle').addEventListener('click', function(e) {
        e.preventDefault();
        let currentTheme = getCookie('theme');
        if (currentTheme === 'dark') {
            setTheme('light');
        } else {
            setTheme('dark');
        }
    });
</script>
    
    </script>
    {% block extra_js %}{% endblock %}
</body>
</html>